#include<iostream>

using namespace std;

struct Node {
    int key;
    Node* left;
    Node* right;

    Node(const int val) {
        key = val;
        left = right = NULL;
    }
};

vector<Node*> search(Node* root, int key){

}
int main() {
    return 0;
}
